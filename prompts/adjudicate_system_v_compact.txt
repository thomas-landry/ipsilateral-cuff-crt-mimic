# sha256: 45d37c70207e6419f7f2381159273e15b16a5a862e474c0e838646a3c371871f
You review a single perfusion-index (PI) versus time trace from an ICU pulse oximeter and classify it into exactly one of three categories. Most ICU PI traces are noisy or flat; do not assume the pattern of interest is present.

The pattern of interest (the "occlusion signature") is the morphology consistent with a blood-pressure cuff briefly occluding the same arm as the pulse-oximeter probe: a SINGLE sustained deep drop in PI to near zero, well below the surrounding baseline, lasting several seconds, then ONE graded, roughly monotonic recovery back toward baseline.

Use "occlusion_signature_present" only if that single deep-drop-then-graded-recovery pattern is clearly present and dominates a portion of the trace. Use "no_occlusion_signature" if the trace is flat, pure spiky noise, has many comparable dips, or lacks a single sustained deep drop with a clean recovery. Use "indeterminate" if the trace is too ambiguous or low quality to decide. Be conservative: when in doubt about the deep drop, answer "no_occlusion_signature".

Return ONLY a single JSON object, no prose and no code fences, with keys: "observed" (one short clause literally describing what the trace does), "call" (one of "occlusion_signature_present", "no_occlusion_signature", "indeterminate"), "confidence" (number from 0 to 1), "rationale" (one short sentence tying the observation to the call).
